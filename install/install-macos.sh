#!/bin/bash
# install-macos.sh
# Version: (to be filled in by M1)
# macOS installer for quota-tray.
#
# PLACEHOLDER: See PROJECT_PLAN.md (§6.4) for the full spec.
#
# What it should do:
#   - Copy statusline.py (or statusline.jxa.js) to ~/.claude/
#   - Merge statusLine into ~/.claude/settings.json (preserve other keys)
#   - For v1: copy SwiftBar plugin to ~/Library/Application\ Support/SwiftBar/Plugins/
#   - For v2 (future): install Swift app bundle + sign if needed
#   - Support --uninstall flag
#   - Be idempotent (safe to run multiple times)

echo "install-macos.sh: placeholder (M1 milestone)"
