#!/bin/bash
# install-wsl.sh
# Version: (to be filled in by M1)
# WSL installer for quota-tray writer (reader is Windows tray).
#
# PLACEHOLDER: See PROJECT_PLAN.md (§4, §6.3) for the full spec.
#
# What it should do:
#   - Detect Windows home via wslpath and /proc/version (cache result)
#   - Copy statusline.py to ~/.claude/
#   - Merge statusLine into ~/.claude/settings.json (preserve other keys)
#   - Print instructions for Windows tray installation
#   - Support --uninstall flag
#   - Be idempotent (safe to run multiple times)

echo "install-wsl.sh: placeholder (M1 milestone)"
