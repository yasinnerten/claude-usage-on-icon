#!/bin/bash
# quota-tray.15s.sh (SwiftBar/BitBar plugin)
# Version: (to be filled in by M1)
# macOS menu bar plugin for Claude Code plan usage via SwiftBar or BitBar.
#
# PLACEHOLDER: See PROJECT_PLAN.md (§6.4) for the reference approach.
#
# What it should do:
#   - Read usage-cache.json every 15 s (SwiftBar trigger)
#   - Print menu bar line: "31% | color=green" (or "?" if no data)
#   - Dropdown with details: both windows, reset times, "updated N ago", version
#   - Single line status output, then detail lines
#   - No network, no credential access, no writes

echo "quota-tray.15s.sh: placeholder (M1 milestone)"
